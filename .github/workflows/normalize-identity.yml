name: Normalize Tasks Identity

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/normalize-identity.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  normalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Normalize technical identity
        run: python3 tools/normalize_identity.py
      - name: Validate PHP and XML
        run: |
          set -euo pipefail
          find component -name '*.php' -print0 | xargs -0 -n1 php -l
          python3 - <<'PY'
          from pathlib import Path
          import xml.etree.ElementTree as ET
          for path in Path('.').rglob('*.xml'):
              if '.git' not in path.parts:
                  ET.parse(path)
          PY
      - name: Verify clean identity
        run: |
          set -euo pipefail
          test -f component/xdecarotasks.xml
          grep -Fq 'Xdecaro\Component\Tasks' component/xdecarotasks.xml
          if grep -RInE 'com_decarotasks|COM_DECAROTASKS|Decarotasks|component/decarotasks\.xml' --exclude='normalize_identity.py' component AGENTS.md README.md; then
            echo 'Legacy Tasks identity remains.' >&2
            exit 1
          fi
      - name: Commit normalized baseline
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Normalize Tasks to xdecaro identity'
          git push origin HEAD:main

# One-shot normalization workflow; remove after successful migration.
